// for communicate with geodata





$(function () {

    /*
    var rootUrl = "//geodata.solutions/api/api.php?type=getCountries";      

    $.getJSON(rootUrl, function (data) {
        //console.log(data['result']);
        
        var ccountry = "<select onchange='getState();' class='form-control count'>";
        $.each(data['result'], function (key, value) {
            $.each(value, function (keys, vals) {
                ccountry += "<option value='" + keys + "'>" + vals + "</option>";
            });
        });
        ccountry += "</select>";
        //$('.ccam').html(country);
        //$('#countryId').html("567");
    });
    */
    
});